/**
 * Panel Module for Somatiq Redesigned Extension
 * 
 * This module provides custom panels for the redesigned UI.
 * Currently using default panels, but can be extended with custom implementations.
 */
export default function ({ servicesManager, extensionManager, commandsManager }) {
  // For now, we'll use the default panels
  // This can be extended later with custom redesigned panels
  return [];
}