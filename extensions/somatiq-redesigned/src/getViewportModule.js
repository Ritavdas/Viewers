/**
 * Viewport Module for Somatiq Redesigned Extension
 * 
 * This module provides custom viewport components for the redesigned UI.
 * Currently using default viewports, but can be extended with custom implementations.
 */
export default function ({ servicesManager, extensionManager, commandsManager }) {
  // For now, we'll use the default viewports
  // This can be extended later with custom redesigned viewport components
  return [];
}